services:
  app:
    build:
      target: builder
    command: ["npm", "run", "dev"]
    environment:
      NODE_ENV: development
      LOG_LEVEL: info
    volumes:
      - ./src:/app/src:ro

  postgres:
    ports:
      - "55432:5432"