{
  "name": "log-service",
  "version": "1.0.0",
  "private": true,
  "description": "A service that ingests high volumes of structured logs, stores them in PostgreSQL, and exposes query and time-bucketed aggregation APIs.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "build": "tsc && cp -r src/db/migrations dist/db/migrations",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.check.json",
    "kill": "lsof -ti :8080 | xargs kill -9 || true",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "format": "biome format --write src",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:watch": "vitest",
    "test:db": "TEST_DATABASE_URL=postgres://logservice:logservice@127.0.0.1:55432/logs vitest run",
    "dev:up": "docker compose down && docker compose up -d && sleep 10",
    "prod:up": "docker compose down && docker compose -f docker-compose.yml up -d --build && sleep 12"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monazak/log-service.git"
  },
  "bugs": {
    "url": "https://github.com/monazak/log-service/issues"
  },
  "homepage": "https://github.com/monazak/log-service#readme",
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.20.4",
    "@types/pg-copy-streams": "^1.2.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "fastify": "^5.11.2",
    "pg": "^8.22.0",
    "pg-copy-streams": "^7.0.0"
  }
}
